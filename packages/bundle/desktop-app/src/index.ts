/**
 * @deepseek-ai/dsh-desktop-app — the Windows desktop patch layer. The package's
 * runtime behavior lives in `cordis.patch.yml`; this module carries no API.
 * @module @deepseek-ai/dsh-desktop-app
 */

export {}
